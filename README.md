# Frontend - Algo Trading SaaS

Next.js dashboard for login/signup, broker connect, trade execution, strategy signals, copy trading, and live ticks.

## Run locally

1. Install dependencies.
2. Copy `.env.example` to `.env.local`.
3. Start the dev server:

```bash
npm run dev
```
